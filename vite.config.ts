import { defineConfig } from 'vite';

// Serveur de démo (index.html + src/demo.ts). Le build lib est dans vite.lib.config.ts.
// Vite 7 compile le SCSS avec l'API moderne de sass sans option.
export default defineConfig({});
